<Window x:Class="WpfUiTest.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF UI Test - SyncRecord List" Height="500" Width="950"
        MinHeight="400" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackBrush}">

    <Window.Resources>
        <!-- 自定义 ListViewItem 模板：一层 Border + GridViewRowPresenter，无多层叠加 -->
        <Style x:Key="Win11ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="1,6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Margin="12">
        <!-- Top bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="SyncRecord ListView 选中效果测试" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            <TextBox x:Name="TestTextBox" Text="输入搜索文件..."
                     Style="{DynamicResource Win11TextBoxStyle}"
                     Width="200" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <Button x:Name="ThemeToggleButton" Content="🌙 切换浅色" Click="ThemeToggle_Click"
                    Margin="12,0,0,0" Padding="12,4" Cursor="Hand"
                    Background="{DynamicResource ControlHoverGrayBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderThickness="0"/>
        </StackPanel>

        <!-- ListView -->
        <ListView x:Name="SyncRecordList"
                  Background="{DynamicResource WindowBackBrush}"
                  BorderThickness="0"
                  ItemsSource="{Binding SyncRecords}"
                  ItemContainerStyle="{StaticResource Win11ListViewItemStyle}"
                  Loaded="SyncRecordList_Loaded"
                  SelectionChanged="SyncRecordList_SelectionChanged">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{DynamicResource Win11GridViewColumnHeaderStyle}">
                    <GridViewColumn Header="📁" Width="45" DisplayMemberBinding="{Binding TypeIcon}"/>
                    <GridViewColumn Header="时间" Width="145" DisplayMemberBinding="{Binding TimestampDisplay}"/>
                    <GridViewColumn Header="仓库" Width="100" DisplayMemberBinding="{Binding RepoName}"/>
                    <GridViewColumn Header="文件" Width="280" DisplayMemberBinding="{Binding FilePath}"/>
                    <GridViewColumn Header="操作" Width="80" DisplayMemberBinding="{Binding OperationDisplay}"/>
                    <GridViewColumn Header="结果" Width="70" DisplayMemberBinding="{Binding ResultDisplay}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>