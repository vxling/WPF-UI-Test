<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 字体 -->
    <FontFamily x:Key="SystemDefaultFont">Segoe UI</FontFamily>

    <!-- 深色主题配色 -->
    <SolidColorBrush x:Key="DataGridHeaderBackground" Color="#FF3C3C3C"/>
    <SolidColorBrush x:Key="HeaderHoverBg" Color="#FF484848"/>
    <SolidColorBrush x:Key="HeaderPressedBg" Color="#FF555555"/>
    <SolidColorBrush x:Key="DataGridHeaderForeground" Color="#FFE5E5E5"/>
    <SolidColorBrush x:Key="DataGridRowBackground" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="DataGridRowAlternatingBackground" Color="#FF252525"/>
    <SolidColorBrush x:Key="DataGridRowSelectedBackground" Color="#FF1F4A78"/>
    <SolidColorBrush x:Key="DataGridRowSelectedForeground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="RowSelectedBorder" Color="#FF4A86C2"/>
    <SolidColorBrush x:Key="RowHoverBg" Color="#FF383838"/>
    <SolidColorBrush x:Key="RowSelectedBorderBrush" Color="#FF4A86C2"/>

    <!-- 基础刷子 -->
    <SolidColorBrush x:Key="WindowBackBrush" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFE5E5E5"/>
    <SolidColorBrush x:Key="ControlHoverGrayBrush" Color="#FF383838"/>

    <!-- 文本框样式 -->
    <SolidColorBrush x:Key="TextBoxBackground" Color="#FF2C2D30"/>
    <SolidColorBrush x:Key="TextBoxForeground" Color="#FFE5E5E5"/>
    <SolidColorBrush x:Key="TextBoxBorderBrush" Color="#FF3F3F3F"/>
    <SolidColorBrush x:Key="TextBoxFocusBorderBrush" Color="#FF60CDFF"/>

    <!-- GridView 表头样式：完全自定义模板，hover/pressed 在 ControlTemplate.Triggers 内控制 -->
    <Style x:Key="Win11GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderForeground}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="BorderBrush" Value="#FF3F3F3F"/>
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="FontFamily" Value="{StaticResource SystemDefaultFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridViewColumnHeader">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{DynamicResource DataGridHeaderBackground}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <Thumb x:Name="PART_HeaderGripper"
                               Width="4"
                               HorizontalAlignment="Right"
                               Cursor="SizeWE"
                               Opacity="0"
                               Background="{TemplateBinding BorderBrush}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HeaderHoverBg}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HeaderPressedBg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 文本框样式 -->
    <Style x:Key="Win11TextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBoxForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontFamily" Value="{StaticResource SystemDefaultFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource TextBoxFocusBorderBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>